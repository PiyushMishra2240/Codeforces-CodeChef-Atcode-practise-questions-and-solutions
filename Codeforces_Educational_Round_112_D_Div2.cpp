#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
//Q) Find the number of unique pairs [i, j] such that a[i] + a[j] = t
int main()
{
    #ifndef ONLINE_JUDGE
         freopen("input.txt","r",stdin);
         freopen("output.txt","w",stdout);
    #endif
    ios::sync_with_stdio(0);
    cin.tie(0);

    ll n,m;
    cin>>n>>m;
    string s;
    cin>>s;
    string c1="abc",c2="bac",c3="bca",c4="acb",c5="cab",c6="cba";
    
    return 0;
}